providers = ["node"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "cd ../frontend && npm ci && npm run build && cp -r dist ../backend/dist"]

[start]
cmd = "npm start"
